[project]
name = "scraper"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "aprilflow>=0.1.0",
    "scrapy>=2.11.0",
    "scrapy-playwright>=0.0.34",
]

[project.scripts]
scraper = "scraper.__main__:main"
